[project]
name = "frontend"
version = "0.1.0"
description = "Add a short description here"
authors = ["Yousifff <yousifsayer@gmail.com>"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]
admin = "pixi run streamlit run pages/admin.py"
index = "pixi run streamlit run pages/index.py"

[dependencies]
streamlit = ">=1.33.0,<1.34"

[pypi-dependencies]
st-pages = "*"
